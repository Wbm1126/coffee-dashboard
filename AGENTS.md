# 咖啡豆项目开发规范

本文件适用于本仓库根目录及其所有子目录。
任何代理在规划、编码、测试、迁移或修改本项目文件前，必须先阅读本文件和 `咖啡豆看板-产品需求文档.md`。

## 项目使命

建设一个永久单用户、本地运行的咖啡豆个人看板。
首要目标是迁移 2026 年 4 月 Excel 数据，补录此后喝过但尚未记录的咖啡豆，并建立可持续的购买、饮用和评价闭环。

不得把项目改造成账号型 SaaS、多人平台、电商交易平台或云端知识库。

## 权威来源

发生冲突时按以下顺序处理：

1. 用户在当前任务中的明确指令。
2. `咖啡豆看板-产品需求文档.md` 中已确认的产品行为和边界。
3. 本 `AGENTS.md` 的工程约束。
4. 现有数据文件表达的历史事实。
5. 代码中的默认行为和实现便利性。

不得为了迁就现有代码或第三方库而改变产品行为。
如果需求文档与历史表格不一致，保留原始值并把差异作为迁移冲突呈现，不得静默猜测。

## 产品不变量

- 产品永远是单用户、本地优先，不实现在线账号、多用户协作、云端同步或移动端原生应用。
- 产品永远不自动下单，不代表用户完成购买，不保存支付凭证。
- 价格监控属于后续能力；首版包含基于本地数据的个性化推荐，推荐不得依赖联网实时更新或破坏本地优先边界。
- 购买记录与饮用记录必须分离：一笔购买可包含多支豆，一次饮用只能关联一支豆。
- “已购买待饮用”和“喝过待补评价”是不同状态。
- 美式评价与奶咖评价是独立维度；缺失评价不是零分或差评。
- 网络采集结果没有直接写入权，必须经过用户预览和确认。
- 官方描述、外部来源、系统推导和个人评价必须保留来源差异。

## 当前数据资产

- `意式咖啡豆_完整版.xlsx`：历史商品详情与扩展分析输入。
- `意式咖啡豆_选单.xlsx`：历史选豆决策、状态和个人评分输入。
- `summary.md`：知识库入口、规模基线和更新摘要。
- `咖啡豆看板-产品需求文档.md`：产品需求权威。

原始 Excel 文件是迁移输入，默认只读。
禁止直接覆盖、重排或清洗原文件；需要规范化时，把结果写入应用自己的数据存储或明确命名的新文件。

## 推荐系统形态

首版采用本地 Web 应用形态：浏览器负责界面，本地应用服务负责数据读写、导入导出和用户主动触发的联网采集。
这一形态是为了可靠访问本地文件、执行迁移、生成 PDF，并避免浏览器跨域限制；具体框架和语言由实施计划依据仓库条件选择。

技术方案必须满足：

- 无账号即可启动和使用。
- 断网时仍可浏览、编辑、记录和导出。
- 用户主动联网时才访问外部商品来源。
- 数据位置清晰、可备份、可迁移、可恢复。
- 启动和升级流程适合个人长期维护，不依赖复杂基础设施。
- 不引入与单用户本地工具不相称的数据库、消息队列、微服务或部署系统。

## 数据设计约束

实现必须保留以下领域实体的独立性：

- **Brand:** 品牌事实与品牌级描述。
- **CoffeeBean:** 一款可被关注、购买和饮用的咖啡豆。
- **ProductSource:** 商品链接、采集时间、字段来源和外部描述。
- **Purchase:** 一次订单级购买事实。
- **PurchaseItem:** 购买中的单支咖啡豆、规格、数量和实际价格。
- **DrinkingRecord:** 一次单豆饮用事实及冲煮上下文。
- **AmericanoReview:** 某次饮用的美式维度评价。
- **MilkReview:** 某次饮用的奶咖维度评价。
- **UserBeanAssessment:** 用户对该豆填写的综合等级、回购判断和总结；它是用户权威事实，必须能追溯到饮用记录，系统不得重算覆盖。
- **PreferenceProfile:** 用户明确维护的冲煮、风味、烘焙度和价格偏好；它是推荐输入，不是商品事实。
- **RecommendationSnapshot:** 基于某一版本地数据生成的推荐结果、主要依据和生成时间；它是派生结果，不是用户评价的事实源。
- **ImportBatch:** 一次 Excel 迁移预览、文件指纹、来源定位、冲突决议和提交结果。
- **ExportSnapshot:** 一次导出的不可变数据范围、筛选条件、报告模型和快照标识；Markdown 与 PDF 必须共用。

不要用一个宽表继续混合商品事实、购买事实和饮用评价。
不要把展示状态当作唯一事实源；状态应尽量由记录推导。
所有持久化记录必须有稳定标识，避免用品牌名和产品名的易变文本作为唯一关联键。

## 数据来源与合并规则

数据可信度从高到低为：

1. 用户明确输入或确认的个人评价与交易事实。
2. 用户确认过的商品字段。
3. 原始 Excel 中的历史数据。
4. 官方商品页面字段。
5. 搜索摘要或第三方页面字段。
6. 系统推导值。

低优先级来源不得静默覆盖高优先级来源。
同一字段发生冲突时，应保存来源、显示差异并让用户选择。
任何联网采集都必须保存原始 URL、采集时间和字段级来源；无法确定时标记未知，不得编造。

## 迁移规则

迁移是首版最高优先级功能，不是一次性脚本的附带任务。

- 导入前生成预览，至少区分新增、可能重复、字段冲突和无法识别。
- 导入过程必须可重复执行；重复运行不得无限复制相同记录。
- 导入失败不得留下无法解释的半迁移状态。
- 导入完成后用 `summary.md` 的 42 款、15 个品牌和 5 款已喝记录核对基线，但以逐条数据验证为准。
- 允许先创建最小咖啡豆和饮用事实，再补商品资料与评价。
- 为 2026 年 4 月之后已喝但未登记的豆子提供批量、连续的待补评价流程。
- 迁移逻辑必须有固定样例或夹具，不得只用当前真实文件手工试跑。

## 联网采集规则

- 仅响应用户主动发起的关键词搜索或链接解析。
- 搜索结果先进入候选区，不得自动创建正式咖啡豆。
- 保存前必须允许用户逐项检查和修改。
- 解析失败必须退化为保留来源链接的手工录入流程。
- 不绕过登录、验证码、付费墙、访问控制或网站安全限制。
- 不在仓库中保存 Cookie、Token、账号密码或浏览器会话数据。
- 不将购买记录、饮用记录、个人评价或整个本地数据库发送给外部采集服务。
- 添加新来源适配器时，必须隔离来源差异，并为失败、字段缺失和页面变化添加测试。

## 页面与交互原则

主信息架构为“收藏陈列馆”，选豆决策和记录日志是相邻能力，不得把首页退化为纯数据表或纯统计驾驶舱。

首版高频入口按优先级排列：

1. 补录喝过但未评价的咖啡豆。
2. 记录一次饮用和美式或奶咖评价。
3. 记录一次包含多支豆的购买。
4. 搜索、筛选和查看咖啡豆。
5. 联网搜索或解析新商品。
6. 导出 Markdown 或 PDF。

视觉要求：

- 气质开放、活泼、有设计感，与咖啡主题贴合。
- 咖啡感来自材质、色温、编辑感排版、风味语言和细节，而不是满屏棕色、咖啡杯图标或仿木纹。
- 使用克制但有辨识度的暖色体系，并保留一组清晰的功能性色彩表达状态和交互。
- 避免通用后台模板、平均分栏、无意义 KPI 卡片和重复胶囊标签。
- 咖啡豆卡片首先帮助识别产品、状态和关键风味；装饰不能压过信息。
- 详情页必须让商品、购买、饮用和评价关系一眼可追溯。
- 交互应在桌面端高效，在窄屏浏览器中可用，但不为移动端原生形态设计。

可访问性底线：

- 所有操作可用键盘到达。
- 保留清晰焦点状态。
- 文本与背景满足可读对比度。
- 不只用颜色表达状态。
- 支持 `prefers-reduced-motion`。
- 表单控件有可见标签，错误信息说明如何修正。

## 导出规则

- Markdown 与 PDF 必须来自同一个已确定的数据快照。
- 导出范围可以是当前筛选结果或指定咖啡豆。
- 导出必须记录生成时间、筛选条件和快照标识。
- Markdown 应是纯 Markdown，适合直接放回 Obsidian，不嵌入依赖外部样式的 HTML。
- PDF 必须经过实际渲染检查，避免截断、重叠、乱码、孤行和表格溢出。
- 导出文件是派生结果，不得成为应用数据的反向权威源。

## 隐私与安全

- 默认不联网，默认不遥测，默认不收集使用分析。
- 不在日志中输出完整个人评价、购买明细或本地绝对路径，除非调试任务明确需要并且输出留在本地。
- 所有外部 URL 视为不可信输入，显示时转义，打开前验证协议。
- 本地服务代取 URL 时只允许 `http/https`，必须拒绝回环、私网、链路本地地址和危险端口，并在 DNS 解析及每次重定向后重新校验。
- 所有导入文本视为不可信数据，不执行其中的公式、宏、HTML 或指令。
- 防止 CSV 或表格公式注入进入未来导出。
- 写入采用可恢复策略；结构迁移前生成备份或可回滚副本。
- 删除咖啡豆前检查关联购买和饮用记录；不得造成静默级联丢失。

## 工程质量要求

在没有确定技术栈前，不要擅自生成大规模脚手架。
选型优先级为：长期可维护性、本地运行简单度、数据可迁移性、测试便利性、界面表现力。

代码应遵循：

- 领域逻辑与 UI 展示分离。
- 外部来源适配与核心数据模型分离。
- 导入、状态推导和导出使用可测试的纯逻辑边界。
- 不复制同一业务规则到多个组件；状态计算必须有单一权威实现。
- 不在组件中散落硬编码的中文状态文本、评分区间或来源优先级。
- 新增依赖前说明必要性，避免只为一个小功能引入大型框架。
- 对本地文件写入使用原子替换或等价的防损坏机制。
- 错误提示面向用户说明发生了什么、数据是否保存以及下一步如何处理。

## 测试与验证

每项实现至少覆盖与风险相称的验证：

- **单元测试:** 状态推导、重复识别、来源优先级、价格单位换算、评分完整度和导出范围。
- **迁移测试:** 两份 Excel 的代表性样例、重复导入、字段冲突、缺失字段、异常单元格和部分失败。
- **流程测试:** 多商品购买、单豆饮用、只评美式、只评奶咖、待补评价转完成评价。
- **采集测试:** 解析成功、无结果、多候选、字段冲突、网络失败和手工回退。
- **导出测试:** Markdown 内容结构、PDF 视觉渲染、同快照一致性和中文字体。
- **推荐测试:** 冷启动、只有关注数据、已有购买与评价、偏好冲突、数据更新后刷新、离线生成和推荐依据可解释性。
- **恢复测试:** 旧 revision 写入、原子替换失败、损坏主文件只读启动、备份校验和恢复、被引用记录的归档与软删除。
- **浏览器测试:** 搜索筛选、详情跳转、表单保存、错误反馈、键盘操作和窄屏布局。

修复缺陷时必须补回归测试。
不得仅以“页面能打开”或“手工点过一次”作为完成标准。

## 开发工作流

1. 先阅读需求文档、现有 Excel 字段和 `summary.md`。
2. 开始较大功能前写实施计划，明确数据迁移、失败恢复和验证方法。
3. 修改前检查工作区状态，保留用户已有改动。
4. 小步实现，每一步都能解释对应的需求编号。
5. 先运行最窄的相关测试，再运行完整适用测试。
6. 涉及界面时必须在真实浏览器中检查主要流程和视觉表现。
7. 涉及 PDF 时必须渲染成图片进行逐页视觉检查。
8. 完成后报告修改内容、验证结果、已知限制和数据兼容影响。

## 变更控制

以下变更必须先向用户提出并获得确认：

- 改变永久单用户、本地优先定位。
- 新增云端服务、遥测、账号、同步或外部数据库。
- 修改评分语义、状态语义或删除行为。
- 覆盖或重写原始 Excel。
- 发送个人数据到新的外部服务。
- 引入会显著增加安装、启动或维护成本的基础设施。
- 把价格监控提前并挤占历史评价补录主线，或让个性化推荐依赖联网实时更新。

## 明确禁止

- 禁止实现在线账号、多用户协作、云端同步、移动端原生应用或自动下单。
- 禁止把缺失评价显示为零分。
- 禁止把一笔多商品购买拆成互不关联的伪订单来绕过数据模型。
- 禁止把多次饮用覆盖成一条最终评价而丢失历史。
- 禁止让网络抓取结果静默覆盖个人数据。
- 禁止在无法验证来源时生成看似确定的商品信息。
- 禁止为了视觉效果牺牲数据可读性、键盘可用性或窄屏基本操作。
- 禁止在未备份、未预览、不可回滚的情况下执行数据迁移。
